@model IEnumerable<lab.LocalCosmosDbApp.ViewModels.PersonViewModel>

@{
    ViewData["Title"] = "Person";
}

<div class="row">
    <div class="col-6">
        <h1>Person</h1>
    </div>
    <div class="col-6 text-right">
        <a href="@Url.Action("Create", "Person")" class="btn btn-sm btn-primary loader-show">Create New</a>
    </div>
</div>

@*<div class="row mt-2">
    <div class="col-12">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.PersonName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.EmailAddress)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.PersonName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EmailAddress)
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>*@

<div class="row mt-2">
    <div class="col-12 text-right mb-2">
        <button class="btn btn-sm btn-outline-info active loader-show btnDataTablesCardViewPerson"><i class="fa fa-th-large"></i> Card</button>
        <button class="btn btn-sm btn-outline-info loader-show btnDataTablesListViewPerson"><i class="fa fa-bars"></i> List</button>
    </div>
    <div class="col-12">
        <div id="dataTablesPersonReplaceDiv" class="mb-50">
            <div style="min-height: 200px">
                <table id="dataTablesPersonCardView" class="table table-striped dataTables" style="width:100%;"></table>
                @*<table id="dataTablesPersonListView" class="table table-striped dataTables" style="width:100%;"></table>*@
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script src="/js/person.js" type="text/javascript"></script>
    <script type="text/javascript">
        $(document).ready(function () {
            Person.InitPerson();
            Person.LoadDataTablesCardView('dataTablesPersonCardView', 5, ("/Person/GetDataTableDataAjax"));
            /*Person.LoadDataTablesListView('dataTablesPersonListView', 5, ("/Person/GetDataTableDataAjax"));*/
            /*Person.LoadDataTablesAction('dataTablesPersonCardView', 'dataTablesPersonListView');*/
            Person.LoadDataTablesAction();
        });
    </script>
}
